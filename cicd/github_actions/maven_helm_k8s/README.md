Deploy an application into a Kubernetes cluster using the following workflows:

On push to Feature branch:

    Run tests

On push to Master branch

    Run tests - Push to docker hub - Deploy to K8s
